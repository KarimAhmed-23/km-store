export const baseUrl =  process.env.REACT_APP_API_KEY;

//environment variables are loaded when your application starts, so if you make changes to your .env file, you'll need to restart your development server for the changes to take effect

//prefixed with REACT_APP_, which is a convention in Create React App (CRA) projects. This prefix ensures that the variables will be exposed to your React application.

// They are typically used to store sensitive information like API keys, database credentials, or URLs that may vary depending on the environment.